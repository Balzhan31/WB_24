import { RouterModule } from '@angular/router';

@NgModule({
  imports: [
    // ... other imports
    RouterModule.forRoot([]), // Add your route configurations inside the parentheses
  ],
  // ...
})
